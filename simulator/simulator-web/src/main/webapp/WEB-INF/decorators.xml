<?xml version="1.0" encoding="UTF-8"?>

<!-- 默认目录 -->
<decorators defaultdir="/WEB-INF/decorators">
	<excludes>  
		<pattern>/financing_detail/modal/*</pattern>
    </excludes> 
    <!-- 只装饰一个页面也可用这种方式定义 -->
    <decorator name="panel" page="panel.jsp"/>

    <!-- 装饰velocity模板 -->
    <!--<decorator name="velocity" page="velocity.vm">
        <pattern>/*</pattern>
    </decorator>-->
</decorators>
